модуль оператор_выбор

импорт "стд::вывод"

// 6.8. Оператор 'выбор'
/*
В операторе выбор по выражению выбор идет по по значению выражения опера-
тора (значение сравнивается со значениями выражений в вариантах).
*/

вход {
    пусть а := 1
    выбор а {
        когда 1: вывод.ф("1")
        другое авария("Переменная со значением 1 не равна 1") 
    }
}
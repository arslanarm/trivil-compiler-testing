модуль оператор_если

// 6.6. Оператор 'если'
/*
Если выражение принимает значение истина,
выполняется Блок первой веткви, в противном случае, выполняется ветвь иначе,
если она задана.
Оператор-если:
    'если' Выражение Блок ('иначе' (Оператор-если | Блок))
*/

вход {
    пусть а := 1
    если а # 1 {
        а := 2
    } иначе {
        а := 3
    }
    надо а = 3 иначе авария("1 != 1 :(")
}